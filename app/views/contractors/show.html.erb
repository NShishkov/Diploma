<div class="container-fluid">
  <p id="notice"><%= notice %></p>
  <div class="col-md-5">
    <h3 class="text-left">
      Информация об исполнителе
      <%= link_to '<i class="glyphicon glyphicon-cog"></i>'.html_safe, edit_contractor_path, {:class => "btn btn-primary btn-link btn-lg"} if @current_role == "contractor" and @profile == @contractor.id %>
    </h3>
    <div class="row-fluid">
      <div class="col-md-3">
        <%= image_tag @contractor.photo.url(:thumb), :class => "img-rounded user-pic" %>
      </div>
      <div class="col-md-9">
        <strong><%= "#{@contractor.surname} #{@contractor.name} #{@contractor.patronymic}" %></strong>
        <br>
        <p> <strong>Местоположение: </strong><%= @contractor.address %></p>
        <p> <strong>Рейтинг: </strong> 
        <%= image_tag(rating(@contractor.rating), size: "100x20") %>
        </p>
      </div>
    </div>
    <div class="row-fluid col-md-12 user">
      <p> <%= @contractor.info %> </p> 
    </div>
    <div class="row">
      <h3 class="text-left">Специализация</h3>
      <section>
        <%= raw @services.map{|s| link_to(s.name, service_path(s.id),:class => "btn btn-xs btn-primary")}.join(" ") %>
      </section>
    </div>
  </div>
  <div class="col-md-7  ">
    <h3 class="text-center">Отзывы об исполнителе</h3>
    <% if @cont_reviews.count == 0 %>  
      <div class="row-fluid col-md-12 text-center">
        <p> На данный момент нет отзывов об этом исполнителе</p> 
      </div>
    <% else %>
      <table class="table">
      <thead>
        <tr>
          <th>Заявка</th>
          <th>Достоинства</th>
          <th>Недостатки</th>
          <th>Комментарий</th>
          <th>Оценка</th>
        </tr>
      </thead>
      <% @cont_reviews.each do |t| %>
      <tbody>
        <tr>
          <td class="col-md-1">
            qq     
          </td>
          <td class="col-md-3">
            qq
          </td>
          <td class="col-md-3">
            qq
          </td>
          <td class="col-md-3">
            qq     
          </td>
          <td class="col-md-2">
            qq   
          </td> 
        </tr>
      <% end %>
      </tbody>   
    </table>
    <% end %>
  </div>
</div>
<div class="row"></div>
