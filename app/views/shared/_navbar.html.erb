<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="glyphicons glyphicons-car-rental"></span>
          </button>
      <a class="navbar-brand" href="#">AutoProfi</a>

    </div>
   
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to('Все задания', :controller => :tasks,
           :action => :index) %></li>    
        <li ><%= link_to('Исполнители', :controller => :contractors,
           :action => :index) %></li>      
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Мой профиль<b class="caret"></b></a>
          <ul class="dropdown-menu">
            
            <li><%= link_to "Редактировать профиль", edit_client_path(current_user.role_users.first.us_role_id) if current_user != nil and current_user.role_users.size != 0 and current_user.roles.first.name == "client"%> </li>
            <li><%= link_to "Редактировать профиль", edit_contractor_path(current_user.role_users.first.us_role_id) if current_user != nil and current_user.role_users.size != 0 and current_user.roles.first.name == "contractor"  %></li>
            <li><a href="#">Role: <%= current_user.roles.first.name if current_user != nil and current_user.roles.size != 0 %> </a></li>
            <li><a href="#">Мои отзывы</a></li>
            <li><%= link_to "Сменить логин\\пароль", edit_user_path(current_user.id) if current_user != nil %></li>
            <li class="divider"></li>
            <li><%= link_to "Выйти из системы", :logout, method: :post if current_user != nil %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
