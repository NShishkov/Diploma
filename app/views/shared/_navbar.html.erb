<span class="glyphicon glyphicon-search"></span>

<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
     <%= link_to image_tag('brand.png', size: "50x50"), home_path, {:class => "navbar-brand"}%>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
   
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to '<i class="glyphicon glyphicon-th-list"></i> Все заявки'.html_safe, tasks_path, {:class => "btn btn-primary btn-link btn-lg"} %></li>   
        <li><%= link_to '<i class="glyphicon glyphicon-user"></i> Исполнители'.html_safe, contractors_path, {:class => "btn btn-primary btn-link btn-lg"} %></li>       
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to '<i class="glyphicon glyphicon-wrench"></i> Создать заявку'.html_safe, new_task_path, {:class => "btn btn-primary btn-link btn-lg"} if @role == "client"%> </li>
        <li><%= link_to '<i class="glyphicon glyphicon-home"></i> Личный кабинет'.html_safe, client_path(@profile), {:class => "btn btn-primary btn-link btn-lg"} if @role == "client"%> </li>
        <li><%= link_to '<i class="glyphicon glyphicon-user"></i> Контактные данные'.html_safe, contractor_path(@profile), {:class => "btn btn-primary btn-link btn-lg"} if @role == "contractor"  %></li>
        <li><%= link_to '<i class="glyphicon glyphicon-user"></i> Выбрать роль'.html_safe, choose_role_path, {:class => "btn btn-primary btn-link btn-lg"} if @role == "new_user" %></li>
        <li><%= link_to'<i class="glyphicon glyphicon-log-out"></i> Выйти из системы'.html_safe, logout_path,{:class => "btn btn-primary btn-link btn-lg",  method: :post} %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

